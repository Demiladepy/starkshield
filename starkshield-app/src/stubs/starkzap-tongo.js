/** Browser bundle stub — real Tongo runs on starkshield-mcp with TONGO_* env. */
export class TongoConfidential {
  constructor() {
    throw new Error(
      "TongoConfidential is not bundled in the StarkShield web client. Use starkshield-mcp for confidential_* tools.",
    );
  }
}
