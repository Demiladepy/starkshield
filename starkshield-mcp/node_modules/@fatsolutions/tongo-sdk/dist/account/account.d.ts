import { BigNumberish, CairoOption, RpcProvider, TypedContractV2 } from "starknet";
import { AEBalance } from "../ae_balance.js";
import { Audit, ExPost } from "../operations/audit.js";
import { FundOperation } from "../operations/fund.js";
import { RagequitOperation } from "../operations/ragequit.js";
import { RollOverOperation } from "../operations/rollover.js";
import { TransferOperation } from "../operations/transfer.js";
import { WithdrawOperation } from "../operations/withdraw.js";
import { tongoAbi } from "../tongo.abi.js";
import { CipherBalance, GeneralPrefixData, PubKey, TongoAddress } from "../types.js";
import { AccountState, FundDetails, IAccount, RagequitDetails, RawAccountState, TransferDetails, WithdrawDetails, RolloverDetails } from "./account.interface.js";
import { AccountEvents, AccountFundEvent, AccountRagequitEvent, AccountRolloverEvent, AccountTransferInEvent, AccountTransferOutEvent, AccountWithdrawEvent } from "./events.js";
type TongoContract = TypedContractV2<typeof tongoAbi>;
export declare class Account implements IAccount {
    publicKey: PubKey;
    pk: bigint;
    provider: RpcProvider;
    Tongo: TongoContract;
    /** @deprecated Use rpc url for the third argument. */
    constructor(pk: BigNumberish | Uint8Array, contractAddress: string, provider: RpcProvider);
    constructor(pk: BigNumberish | Uint8Array, contractAddress: string, rpcUrl: string);
    tongoAddress(): TongoAddress;
    static tongoAddress(pk: BigNumberish | Uint8Array): TongoAddress;
    nonce(): Promise<bigint>;
    state(): Promise<AccountState>;
    rawState(): Promise<RawAccountState>;
    rate(): Promise<bigint>;
    bit_size(): Promise<number>;
    erc20ToTongo(erc20Amount: bigint): Promise<bigint>;
    tongoToErc20(tongoAmount: bigint): Promise<bigint>;
    auditorKey(): Promise<CairoOption<PubKey>>;
    createAuditPart(balance: bigint, storedCipherBalance: CipherBalance, prefix_data: GeneralPrefixData): Promise<CairoOption<Audit>>;
    fund(fundDetails: FundDetails): Promise<FundOperation>;
    transfer(transferDetails: TransferDetails): Promise<TransferOperation>;
    ragequit(ragequitDetails: RagequitDetails): Promise<RagequitOperation>;
    withdraw(withdrawDetails: WithdrawDetails): Promise<WithdrawOperation>;
    rollover(rolloverDetails: RolloverDetails): Promise<RollOverOperation>;
    decryptAEBalance(aeBalance: AEBalance, accountNonce: bigint): Promise<bigint>;
    decryptAEHintForPubKey(aeHint: AEBalance, accountNonce: bigint, other: PubKey): Promise<bigint>;
    decryptCipherBalance({ L, R }: CipherBalance, hint?: bigint): bigint;
    generateExPost(to: PubKey, cipher: CipherBalance, sender: string): Promise<ExPost>;
    verifyExPost(expost: ExPost): bigint;
    _diffieHellman(other: TongoAddress): Uint8Array<ArrayBufferLike>;
    computeAEHintForPubKey(amount: bigint, nonce: bigint, pubKey: PubKey): Promise<AEBalance>;
    computeAEHintForSelf(amount: bigint, nonce: bigint): Promise<AEBalance>;
    deriveSymmetricKeyForPubKey(nonce: bigint, other: PubKey): Promise<Uint8Array<ArrayBufferLike>>;
    static parseAccountState(state: Awaited<ReturnType<TongoContract["get_state"]>>): {
        balance: CipherBalance;
        pending: CipherBalance;
        audit: CipherBalance | undefined;
        nonce: bigint;
        aeBalance: AEBalance | undefined;
        aeAuditBalance: AEBalance | undefined;
    };
    getEventsFund(initialBlock: number): Promise<AccountFundEvent[]>;
    getEventsRollover(initialBlock: number): Promise<AccountRolloverEvent[]>;
    getEventsWithdraw(initialBlock: number): Promise<AccountWithdrawEvent[]>;
    getEventsRagequit(initialBlock: number): Promise<AccountRagequitEvent[]>;
    getEventsTransferOut(initialBlock: number): Promise<AccountTransferOutEvent[]>;
    getEventsTransferIn(initialBlock: number): Promise<AccountTransferInEvent[]>;
    getTxHistory(initialBlock: number): Promise<AccountEvents[]>;
}
export {};
