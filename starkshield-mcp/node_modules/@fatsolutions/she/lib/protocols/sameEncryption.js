"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.verify = verify;
exports.verify_with_prefix = verify_with_prefix;
exports._verify = _verify;
exports.prove = prove;
const utils_js_1 = require("../utils.js");
const ElGamal_js_1 = require("./ElGamal.js");
/**
 * Verifies a same encryption proof with explicit challenge.
 * @param {SameEncryptionInputs} inputs - The public inputs containing L1, R1, L2, R2, g, y1, y2
 * @param {SameEncryptionProof} proof - The proof containing AL1, AR1, AL2, AR2, c, sb, sr1, sr2
 * @returns {boolean} True if the proof is valid, false otherwise
 */
function verify(inputs, proof) {
    const { L1, R1, L2, R2, g, y1, y2 } = inputs;
    const { AL1, AR1, AL2, AR2, c, sb, sr1, sr2 } = proof;
    return _verify(L1, R1, L2, R2, g, y1, y2, AL1, AR1, AL2, AR2, c, sb, sr1, sr2);
}
/**
 * Verifies a same encryption proof with challenge computed from prefix.
 * @param {SameEncryptionInputs} inputs - The public inputs containing L1, R1, L2, R2, g, y1, y2
 * @param {SameEncryptionProofWithPrefix} proof - The proof containing AL1, AR1, AL2, AR2, prefix, sb, sr1, sr2
 * @returns {boolean} True if the proof is valid, false otherwise
 */
function verify_with_prefix(inputs, proof) {
    const { L1, R1, L2, R2, g, y1, y2 } = inputs;
    const { AL1, AR1, AL2, AR2, prefix, sb, sr1, sr2 } = proof;
    const commitments = [AL1, AR1, AL2, AR2];
    const c = (0, utils_js_1.compute_challenge)(prefix, commitments);
    return _verify(L1, R1, L2, R2, g, y1, y2, AL1, AR1, AL2, AR2, c, sb, sr1, sr2);
}
/**
 * Verifies that two encryptions for two different keys are valid and that they are encrypting the
 * same amount `b`. Note: We assume here that the two randoms `r1` and `r2` are known by the
 * prover.
 * This proof is just two proof of ElGamal encryption which both use the same value `sb`.
 * `(L1, R1) = (g^b y1^r1, g^r1)`,  `(L2, R2) = (g^b y2^r2, g^r2)`. The protocol runs as follows
 *
 * ```
 *     P:  kb, kr1, kr2 <-- R      sends    AL1=g^kb y1^kr1, AR1=g^kb, AL2=g^kb y2^kr2, AR2=g^kr2
 *     V:          c  <-- R        sends    c
 *     P:  sb  = kb  + c*b         sends    sb
 *     P:  sr1 = kr1 + c*r1        sends    sr1
 *     P:  sr2 = kr2 + c*r2        sends    sr2
 * ```
 *
 * The verifier asserts:
 *
 *  - The correct encryption of `(L1,R1)`
 *
 *  - The correct encryption of `(L2,R2)`
 *
 * notation: (P)rover (V)erifier (R)andom
 *
 * EC_MUL: 10;
 * EC_ADD: 6
 *
 * @param {ProjectivePoint} L1 - Left component of first ElGamal ciphertext
 * @param {ProjectivePoint} R1 - Right component of first ElGamal ciphertext
 * @param {ProjectivePoint} L2 - Left component of second ElGamal ciphertext
 * @param {ProjectivePoint} R2 - Right component of second ElGamal ciphertext
 * @param {ProjectivePoint} g - Generator point
 * @param {ProjectivePoint} y1 - First public key
 * @param {ProjectivePoint} y2 - Second public key
 * @param {ProjectivePoint} AL1 - First left commitment
 * @param {ProjectivePoint} AR1 - First right commitment
 * @param {ProjectivePoint} AL2 - Second left commitment
 * @param {ProjectivePoint} AR2 - Second right commitment
 * @param {bigint} c - Challenge value
 * @param {bigint} sb - Response for message
 * @param {bigint} sr1 - Response for first randomness
 * @param {bigint} sr2 - Response for second randomness
 * @returns {boolean} True if verification succeeds, false if ElGamal verification fails
 */
function _verify(L1, R1, L2, R2, g, y1, y2, AL1, AR1, AL2, AR2, c, sb, sr1, sr2) {
    if (!(0, ElGamal_js_1._verify)(L1, R1, g, y1, AL1, AR1, c, sb, sr1)) {
        return false;
    }
    if (!(0, ElGamal_js_1._verify)(L2, R2, g, y2, AL2, AR2, c, sb, sr2)) {
        return false;
    }
    return true;
}
/**
 * Generates a proof that two encryptions for two different keys are valid and that
 * they are encrypting the same amount `b`. Note: We assume here that the two
 * randoms `r1` and `r2` are known by the prover.
 * This proof is just two proof of ElGamal encryption which both use the same value `sb`.
 * `(L1, R1) = (g^b y1^r1, g^r1)`,  `(L2, R2) = (g^b y2^r2, g^r2)`. The protocol runs as follows
 *
 * ```
 *     P:  kb, kr1, kr2 <-- R      sends    AL1=g^kb y1^kr1, AR1=g^kb, AL2=g^kb y2^kr2, AR2=g^kr2
 *     V:          c  <-- R        sends    c
 *     P:  sb  = kb  + c*b         sends    sb
 *     P:  sr1 = kr1 + c*r1        sends    sr1
 *     P:  sr2 = kr2 + c*r2        sends    sr2
 * ```
 *
 * The verifier asserts:
 *
 *  - The correct encryption of `(L1,R1)`
 *
 *  - The correct encryption of `(L2,R2)`
 *
 * notation: (P)rover (V)erifier (R)andom
 *
 * @param {ProjectivePoint} g - Generator point
 * @param {ProjectivePoint} y1 - First public key
 * @param {ProjectivePoint} y2 - Second public key
 * @param {bigint} message - The message being encrypted
 * @param {bigint} random1 - Randomness for first encryption
 * @param {bigint} random2 - Randomness for second encryption
 * @param {bigint} prefix - Prefix for challenge computation
 * @returns {{inputs: SameEncryptionInputs, proof: SameEncryptionProofWithPrefix}} The public inputs and generated proof
 */
function prove(g, y1, y2, message, random1, random2, prefix) {
    const L1 = g.multiply(message).add(y1.multiply(random1));
    const R1 = g.multiply(random1);
    const L2 = g.multiply(message).add(y2.multiply(random2));
    const R2 = g.multiply(random2);
    const inputs = { L1, R1, L2, R2, y1, y2, g };
    const kb = (0, utils_js_1.generateRandom)();
    const kr1 = (0, utils_js_1.generateRandom)();
    const kr2 = (0, utils_js_1.generateRandom)();
    const AL1 = g.multiply(kb).add(y1.multiply(kr1));
    const AR1 = g.multiply(kr1);
    const AL2 = g.multiply(kb).add(y2.multiply(kr2));
    const AR2 = g.multiply(kr2);
    const commitments = [AL1, AR1, AL2, AR2];
    const c = (0, utils_js_1.compute_challenge)(prefix, commitments);
    const sb = (0, utils_js_1.compute_s)(kb, message, c);
    const sr1 = (0, utils_js_1.compute_s)(kr1, random1, c);
    const sr2 = (0, utils_js_1.compute_s)(kr2, random2, c);
    const proof = { AL1, AR1, AL2, AR2, prefix, sb, sr1, sr2 };
    return { inputs, proof };
}
//# sourceMappingURL=sameEncryption.js.map