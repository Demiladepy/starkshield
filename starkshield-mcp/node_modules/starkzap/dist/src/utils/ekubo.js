export function supportsEkuboChain(chainId) {
    const literal = chainId.toLiteral();
    return literal === "SN_MAIN" || literal === "SN_SEPOLIA";
}
export function getEkuboChainLiteral(chainId, feature) {
    const literal = chainId.toLiteral();
    if (literal === "SN_MAIN" || literal === "SN_SEPOLIA") {
        return literal;
    }
    throw new Error(`Unsupported chain for Ekubo ${feature}: ${literal}`);
}
export function isRecord(value) {
    return typeof value === "object" && value !== null && !Array.isArray(value);
}
export function getEkuboErrorMessageFromPayload(payload) {
    if (!isRecord(payload)) {
        return null;
    }
    return typeof payload.error === "string" ? payload.error : null;
}
//# sourceMappingURL=ekubo.js.map