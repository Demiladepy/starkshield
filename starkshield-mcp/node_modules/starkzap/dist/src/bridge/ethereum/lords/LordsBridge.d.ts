import { CanonicalEthereumBridge } from "../../../bridge/ethereum/canonical/CanonicalEthereumBridge.js";
import type { EthereumTransactionDetails } from "../../../bridge/ethereum/types.js";
import type { Address } from "../../../types/index.js";
import { Amount, EthereumBridgeToken } from "../../../types/index.js";
import type { EthereumWalletConfig } from "../../../bridge/ethereum/types.js";
import type { WalletInterface } from "../../../wallet/index.js";
import { FeeErrorCause } from "../../../types/errors.js";
export declare class LordsBridge extends CanonicalEthereumBridge {
    constructor(bridgeToken: EthereumBridgeToken, config: EthereumWalletConfig, starknetWallet: WalletInterface);
    /**
     * The LORDS L1 bridge has a single-token contract with a different deposit
     * signature: `deposit(uint256 amount, uint256 l2Recipient, uint256 fee)`.
     * Unlike the canonical bridge's `deposit(address token, uint256 amount,
     * uint256 l2Recipient)`, the token address is implicit (one bridge per
     * token) and a fee argument (1 wei) is passed instead. No ETH value is
     * attached to the transaction.
     */
    protected prepareDepositTransactionDetails(recipient: Address, amount: Amount): Promise<EthereumTransactionDetails>;
    /**
     * The LORDS L2 bridge uses `handle_deposit` with a 3-element payload
     * `[recipient, amount_low, amount_high]`, whereas the canonical bridge uses
     * `handle_token_deposit` with a 5-element payload that also includes the L1
     * token address and the sender address.
     */
    protected estimateL1ToL2MessageFee(recipient: Address, amount: Amount): Promise<{
        fee: Amount;
        l2FeeError?: FeeErrorCause;
    }>;
    /**
     * `prepareDepositTransactionDetails` (the only call site for now) is
     * completely overridden, but it is a good practise to maintain that no eth
     * are spent.
     */
    protected getEthDepositValue(_recipient: Address, _amount: Amount): Promise<Amount>;
}
//# sourceMappingURL=LordsBridge.d.ts.map