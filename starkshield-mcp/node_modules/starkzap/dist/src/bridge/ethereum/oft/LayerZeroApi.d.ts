import type { ContractTransaction } from "ethers";
import { type EthereumAddress, ExternalChain } from "../../../types/index.js";
import type { Address, Amount } from "../../../types/index.js";
export interface LayerZeroQuoteFee {
    chainKey: string;
    type: string;
    description: string;
    amount: string;
    address: string;
}
export interface LayerZeroUserStep {
    type: string;
    description: string;
    chainKey: string;
    chainType: string;
    signerAddress: string;
    transaction: {
        encoded: ContractTransaction;
    };
}
export interface LayerZeroQuote {
    id: string;
    routeSteps: {
        type: string;
        srcChainKey: string;
        description: string;
        duration: {
            estimated: string | null;
        };
        fees: LayerZeroQuoteFee[];
    }[];
    fees: LayerZeroQuoteFee[];
    duration: {
        estimated: string | null;
    };
    feeUsd: string;
    feePercent: string;
    srcAmount: string;
    dstAmount: string;
    dstAmountMin: string;
    srcAmountUsd: string;
    dstAmountUsd: string;
    userSteps: LayerZeroUserStep[];
    options: {
        dstNativeDropAmount: string;
    };
    expiresAt: string;
}
interface LayerZeroApiConfig {
    externalTokenAddress: string;
    starknetTokenAddress: Address;
    externalChainKey: ExternalChain;
    apiKey: string;
}
interface QuoteRequestParams {
    srcWalletAddress: string;
    dstWalletAddress: string;
    amount: Amount;
}
export declare class LayerZeroApi {
    private readonly config;
    private readonly fetcher;
    constructor(config: LayerZeroApiConfig, fetcher?: typeof fetch);
    getDepositQuotes(params: QuoteRequestParams): Promise<LayerZeroQuote[]>;
    getApprovalTransaction(quotes: LayerZeroQuote[]): ContractTransaction | null;
    getDepositTransaction(quotes: LayerZeroQuote[]): ContractTransaction | null;
    /**
     * Extract the allowance spender from an approval transaction's calldata.
     * Parses `approve(address,uint256)` to retrieve the spender argument.
     */
    extractSpenderFromApprovalTx(approvalTx: ContractTransaction | null): EthereumAddress | null;
    private getQuotes;
    private getBridgeTransaction;
}
export {};
//# sourceMappingURL=LayerZeroApi.d.ts.map