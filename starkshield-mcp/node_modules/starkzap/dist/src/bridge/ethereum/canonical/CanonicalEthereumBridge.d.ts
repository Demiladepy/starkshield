import { EthereumBridge } from "../../../bridge/ethereum/EthereumBridge.js";
import type { BridgeDepositOptions, EthereumDepositFeeEstimation, EthereumTransactionDetails } from "../../../bridge/index.js";
import { type EthereumAddress, type ExternalTransactionResponse } from "../../../types/index.js";
import { type Address, Amount } from "../../../types/index.js";
import { FeeErrorCause } from "../../../types/errors.js";
export declare class CanonicalEthereumBridge extends EthereumBridge {
    private static readonly DUMMY_SN_ADDRESS;
    private static readonly DEFAULT_ESTIMATED_DEPOSIT_GAS_REQUIREMENT;
    deposit(recipient: Address, amount: Amount, _options?: BridgeDepositOptions): Promise<ExternalTransactionResponse>;
    protected getAllowanceSpender(): Promise<EthereumAddress | null>;
    getDepositFeeEstimate(_options?: BridgeDepositOptions): Promise<EthereumDepositFeeEstimation>;
    protected getEthereumGasPrice(): Promise<bigint>;
    protected prepareDepositTransactionDetails(recipient: Address, amount: Amount): Promise<EthereumTransactionDetails>;
    protected estimateL1ToL2MessageFee(recipient: Address, amount: Amount): Promise<{
        fee: Amount;
        l2FeeError?: FeeErrorCause;
    }>;
    private estimateEthereumGasFeeForTx;
    private estimateEthereumSafeGasLimitForTx;
    protected getEthDepositValue(recipient: Address, amount: Amount): Promise<Amount>;
}
//# sourceMappingURL=CanonicalEthereumBridge.d.ts.map