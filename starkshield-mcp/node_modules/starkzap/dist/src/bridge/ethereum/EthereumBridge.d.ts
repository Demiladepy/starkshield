import type { BridgeDepositOptions, BridgeInterface } from "../../bridge/types/BridgeInterface.js";
import { type Address, Amount, type BridgeDepositFeeEstimation, type EthereumAddress, EthereumBridgeToken, type ExternalTransactionResponse } from "../../types/index.js";
import { type EthereumTokenInterface } from "../../bridge/ethereum/EtherToken.js";
import { type ApprovalFeeEstimation, type EthereumTransactionDetails, type EthereumWalletConfig } from "../../bridge/ethereum/types.js";
import type { InterfaceAbi } from "ethers";
import { Contract, type ContractTransaction, type ContractTransactionResponse, type TransactionRequest } from "ethers";
import type { WalletInterface } from "../../wallet/index.js";
export declare abstract class EthereumBridge implements BridgeInterface<EthereumAddress> {
    protected readonly bridgeToken: EthereumBridgeToken;
    protected readonly config: EthereumWalletConfig;
    readonly starknetWallet: WalletInterface;
    static readonly ALLOWANCE_CACHE_TTL = 60000;
    static readonly GAS_LIMIT_SAFE_MULTIPLIER = 1.5;
    private allowanceCache;
    protected readonly token: EthereumTokenInterface;
    protected readonly bridge: Contract;
    constructor(bridgeToken: EthereumBridgeToken, config: EthereumWalletConfig, starknetWallet: WalletInterface, bridgeAbi?: InterfaceAbi);
    abstract deposit(recipient: Address, amount: Amount, options?: BridgeDepositOptions): Promise<ExternalTransactionResponse>;
    abstract getDepositFeeEstimate(_options?: BridgeDepositOptions): Promise<BridgeDepositFeeEstimation>;
    getAvailableDepositBalance(account: EthereumAddress): Promise<Amount>;
    getAllowance(): Promise<Amount | null>;
    protected abstract getAllowanceSpender(): Promise<EthereumAddress | null>;
    protected abstract getEthereumGasPrice(): Promise<bigint>;
    protected approveSpendingOf(amount: Amount): Promise<void>;
    protected execute(tx: TransactionRequest): Promise<ContractTransactionResponse>;
    protected populateTransaction(details: EthereumTransactionDetails): Promise<ContractTransaction>;
    protected ethAmount(value: bigint): Amount;
    protected estimateApprovalFee(): Promise<ApprovalFeeEstimation>;
    protected clearCachedAllowance(): void;
    private setCachedAllowance;
    private updateAllowanceFromReceipt;
}
//# sourceMappingURL=EthereumBridge.d.ts.map