import { type Address, Amount, type EthereumAddress, type ExternalTransactionResponse } from "../../../types/index.js";
import type { BridgeDepositOptions } from "../../../bridge/types/BridgeInterface.js";
import type { CCTPDepositFeeEstimation } from "../../../bridge/index.js";
import { EthereumBridge } from "../../../bridge/ethereum/EthereumBridge.js";
export declare class CCTPBridge extends EthereumBridge {
    private static readonly MAINNET_TOKEN_MESSENGER;
    private static readonly SEPOLIA_TOKEN_MESSENGER;
    private static DEFAULT_CCTP_DEPOSIT_GAS;
    private static TOKEN_MESSENGER_INTERFACE;
    private static readonly DUMMY_SN_ADDRESS;
    private static readonly ZERO_ETH;
    private readonly cctpFees;
    deposit(recipient: Address, amount: Amount, options?: BridgeDepositOptions): Promise<ExternalTransactionResponse>;
    getDepositFeeEstimate(options?: BridgeDepositOptions): Promise<CCTPDepositFeeEstimation>;
    protected getAllowanceSpender(): Promise<EthereumAddress>;
    protected getEthereumGasPrice(): Promise<bigint>;
    private usdcAmount;
    private createDepositForBurnTransaction;
    private calculateMaxFee;
}
//# sourceMappingURL=CCTPBridge.d.ts.map