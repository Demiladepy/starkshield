import type { BridgeInterface } from "../../bridge/types/BridgeInterface.js";
import { type Address, Amount, type ExternalTransactionResponse, type SolanaAddress, type SolanaBridgeToken } from "../../types/index.js";
import type { SolanaDepositFeeEstimation, SolanaWalletConfig } from "../../bridge/solana/types.js";
import type { WalletInterface } from "../../wallet/index.js";
export declare class SolanaHyperlaneBridge implements BridgeInterface<SolanaAddress> {
    private readonly bridgeToken;
    private readonly config;
    readonly starknetWallet: WalletInterface;
    private readonly hyperlane;
    private readonly multiProvider;
    private readonly warpCore;
    private readonly solanaToken;
    private readonly starknetChain;
    private readonly solanaChain;
    private constructor();
    static create(bridgeToken: SolanaBridgeToken, config: SolanaWalletConfig, starknetWallet: WalletInterface): Promise<SolanaHyperlaneBridge>;
    deposit(recipient: Address, amount: Amount): Promise<ExternalTransactionResponse>;
    getDepositFeeEstimate(): Promise<SolanaDepositFeeEstimation>;
    getAvailableDepositBalance(account: SolanaAddress): Promise<Amount>;
    getAllowance(): Promise<Amount | null>;
    private estimateDepositInterchainFee;
    private estimateDepositLocalFee;
    private solAmount;
}
//# sourceMappingURL=SolanaHyperlaneBridge.d.ts.map