import { BridgeToken } from "../../types/bridge/bridge-token.js";
import type { BridgeDepositOptions } from "../../bridge/types/BridgeInterface.js";
import { type ConnectedExternalWallet } from "../../connect/index.js";
import type { WalletInterface } from "../../wallet/index.js";
import type { BridgeOperatorInterface } from "../../bridge/operator/BridgeOperatorInterface.js";
import { type Address, type Amount, type BridgeDepositFeeEstimation, type BridgingConfig, type ExternalTransactionResponse } from "../../types/index.js";
export declare class BridgeOperator implements BridgeOperatorInterface {
    private readonly starknetWallet;
    private readonly bridgingConfig?;
    private cache;
    constructor(starknetWallet: WalletInterface, bridgingConfig?: BridgingConfig | undefined);
    deposit(recipient: Address, amount: Amount, token: BridgeToken, externalWallet: ConnectedExternalWallet, options?: BridgeDepositOptions): Promise<ExternalTransactionResponse>;
    getDepositBalance(token: BridgeToken, externalWallet: ConnectedExternalWallet): Promise<Amount>;
    getDepositFeeEstimate(token: BridgeToken, externalWallet: ConnectedExternalWallet, options?: BridgeDepositOptions): Promise<BridgeDepositFeeEstimation>;
    getAllowance(token: BridgeToken, externalWallet: ConnectedExternalWallet): Promise<Amount | null>;
    private bridge;
    private createBridge;
    private createEthereumBridge;
    private createSolanaBridge;
}
//# sourceMappingURL=BridgeOperator.d.ts.map