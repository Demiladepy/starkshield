import type { BridgeInterface } from "../../bridge/types/BridgeInterface.js";
import { BridgeToken } from "../../types/index.js";
import { type ConnectedExternalWallet } from "../../connect/index.js";
export declare class BridgeCache {
    private readonly cache;
    get(token: BridgeToken, wallet: ConnectedExternalWallet): Promise<BridgeInterface> | undefined;
    set(token: BridgeToken, wallet: ConnectedExternalWallet, bridge: Promise<BridgeInterface>): void;
    private evictOldest;
    private touch;
    private key;
}
//# sourceMappingURL=BridgeCache.d.ts.map